<!DOCTYPE html>
<html>
<head>
  <title>the hacker school confessional</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <link href='https://fonts.googleapis.com/css?family=Press+Start+2P' rel='stylesheet' type='text/css'>
  <link rel="shortcut icon" href="favicon.ico"/>
  <script src="http://js.pusher.com/1.12/pusher.min.js"></script>
</head>
<body>
  <div id="counter">
    <%= (@session_count + 2).to_s + " sinners in the booth" %>
  </div>
  <div id="wrapper" class="center">
    <h1>
      <%= link_to(confessions_path) do %>
        $ the hacker school confessional<span class="blink cursor">&nbsp;</span>
      <% end %>
    </h1>

<%= yield %>

  </div>

  <script type="text/javascript" language="javascript">
    $(document).ready(function()
    {
      $('.blink').blink(); // default is 500ms blink interval.
    });
  </script>
  <script type="text/javascript">
    var pusher = new Pusher('3959abe8b63fee2973a9'); // Replace with your app key
    var channel = pusher.subscribe('my-channel');
    channel.bind('my-event', function(data) {
      alert('An event was triggered with message: ' + data.message);
    });
  </script>
</body>
</html>
